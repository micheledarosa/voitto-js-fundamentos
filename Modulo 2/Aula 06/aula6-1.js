const numeros = [1, 56, -198, 0, 234];

// Se existe o número no array (true ou false)
console.log(numeros.includes(234));

// Posição do elemento 0 no array
console.log(numeros.indexOf(0));

// Quando um elemento não existir no array, retorna -1
console.log(numeros.indexOf(90));